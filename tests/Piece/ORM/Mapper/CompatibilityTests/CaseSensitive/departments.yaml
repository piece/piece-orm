# $Id$

methods:

  findAllWithEmployees1:
    relationships:
      employees:
        table: employees
        type: oneToMany

  findAllWithEmployees2:
    relationships:
      employees:
        table: employees
        type: oneToMany
        column: departments_id
        referencedColumn: id

  findAllWithOrderedEmployees:
    relationships:
      employees:
        table: employees
        type: oneToMany
        orderBy: id DESC

  findWithEmployeesByName:
    query: |
      SELECT
        *
      FROM
        $__table
      WHERE
        name = $name
    relationships:
      employees:
        table: employees
        type: oneToMany
        orderBy: id DESC

  update:
    relationships:
      employees:
        table: employees
        type: oneToMany

  delete:
    relationships:
      employees:
        table: employees
        type: oneToMany

# Local Variables:
# mode: conf-colon
# coding: iso-8859-1
# tab-width: 2
# indent-tabs-mode: nil
# End:
